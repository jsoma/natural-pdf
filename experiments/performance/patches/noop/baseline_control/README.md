# Baseline Control Patch

Track: control

This patch intentionally does nothing. Use it to verify that `--patch`,
`--experiment-label`, and experiment metadata are working before comparing real
candidate patches.
