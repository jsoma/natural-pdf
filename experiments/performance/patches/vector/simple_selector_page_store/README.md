# Vector: Simple Selector Page Store

Reuses `PageArrayView` arrays for simple page-level selector branches. This
tests whether the prior simple-selector regression was caused by rebuilding
arrays for every selector call.
